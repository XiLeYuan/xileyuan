<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    android:background="@drawable/bg_matchmaker_card"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    android:padding="16dp">

        <!-- 顶部：头像和信息 -->
        <LinearLayout
            android:id="@+id/llHeader"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            android:orientation="horizontal">

            <!-- 头像容器（包含头像和认证标识） -->
            <FrameLayout
                android:layout_width="60dp"
                android:layout_height="60dp"
                android:layout_marginStart="0dp">

                <!-- 头像 -->
                <com.xly.middlelibrary.widget.LYRoundImageView
                    android:id="@+id/ivAvatar"
                    android:layout_width="60dp"
                    android:layout_height="60dp"
                    android:scaleType="centerCrop"
                    android:src="@mipmap/head_img" />

                <!-- 认证标识（头像右下角） -->
                <ImageView
                    android:id="@+id/ivVerified"
                    android:layout_width="18dp"
                    android:layout_height="18dp"
                    android:layout_gravity="bottom|end"
                    android:layout_marginEnd="1dp"
                    android:layout_marginBottom="1dp"
                    android:src="@mipmap/verified_icon2"
                    android:visibility="visible" />

            </FrameLayout>

            <!-- 红娘信息 -->
            <androidx.constraintlayout.widget.ConstraintLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="12dp"
                android:layout_marginTop="4dp">

                <!-- 姓名 -->
                <TextView
                    android:id="@+id/tvName"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toTopOf="parent"
                    android:text="张红娘"
                    android:textSize="@dimen/text_size_h4"
                    android:textStyle="bold"
                    android:textColor="@color/text_primary" />


                <!-- 地址信息 - 放在名字下方 -->
                <TextView
                    android:id="@+id/tvLocation"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toBottomOf="@id/tvName"
                    android:layout_marginTop="4dp"
                    android:text="固始"
                    android:textSize="@dimen/text_size_body_small"
                    android:textColor="@color/text_secondary" />

                <!-- 用户数量 - 右上角，与名字和地点整体竖直居中 -->
                <TextView
                    android:id="@+id/tvUserCount"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintTop_toTopOf="@id/tvName"
                    app:layout_constraintBottom_toBottomOf="@id/tvLocation"
                    android:text="128位用户"
                    android:textSize="@dimen/text_size_body_small"
                    android:gravity="center_vertical"
                    android:clickable="true"
                    android:focusable="true"
                    android:foreground="?attr/selectableItemBackground"
                    android:contentDescription="查看用户资源" />

            </androidx.constraintlayout.widget.ConstraintLayout>


        </LinearLayout>

        <!-- 简介 -->
        <TextView
            android:id="@+id/tvDescription"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/llHeader"
            android:layout_marginTop="8dp"
            android:text="10年专业经验，成功撮合500+对新人，擅长高端客户匹配"
            android:textSize="@dimen/text_size_body_small"
            android:textColor="@color/text_secondary"
            android:maxLines="2"
            android:ellipsize="end"
            android:lineSpacingExtra="4dp" />

        <!-- 标签和箭头按钮 -->
        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/clTagsContainer"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tvDescription"
            android:layout_marginTop="12dp">

            <!-- 标签 -->
            <LinearLayout
                android:id="@+id/llTags"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toStartOf="@id/ivArrowButton"
                android:orientation="horizontal">

            </LinearLayout>

            <!-- 箭头按钮 - 最右边 -->
            <ImageView
                android:id="@+id/ivArrowButton"
                android:layout_width="20dp"
                android:layout_height="20dp"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="@id/llTags"
                app:layout_constraintBottom_toBottomOf="@id/llTags"
                android:src="@drawable/ic_arrow_right"
                app:tint="@color/brand_primary"
                android:clickable="true"
                android:focusable="true"
                android:foreground="?attr/selectableItemBackground"
                android:contentDescription="查看用户资源" />

        </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.constraintlayout.widget.ConstraintLayout>